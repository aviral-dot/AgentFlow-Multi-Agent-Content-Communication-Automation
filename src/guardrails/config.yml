models:
  - type: main
    engine: nim
    model: meta/llama-3.1-8b-instruct

enable_rails_exceptions: True

rails:
  input:
    flows:
      - self check input

  output:
    flows:
      - self check output
